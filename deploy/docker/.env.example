# Dinotty Docker 环境变量

# 主机映射端口
DINOTTY_PORT=8999

# 容器内监听地址（端口发布由 docker-compose 控制）
DINOTTY_BIND_ADDR=0.0.0.0

# 认证 Token（留空则自动生成）
DINOTTY_TOKEN=

# 日志级别
RUST_LOG=info

# 时区
TZ=UTC

# 工作区目录（挂载到容器 /workspace）
WORKSPACE_DIR=./workspace
